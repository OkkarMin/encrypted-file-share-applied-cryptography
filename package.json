{
  "name": "encrypted-file-share-applied-cryptography",
  "version": "1.5.0",
  "scripts": {
    "release": "standard-version",
    "installDeps": "cd socket-server && npm install && cd ../nextjs-client && npm install && cd .. && npm install",
    "dev:socket-server": "cd socket-server && npm run watch",
    "dev:nextjs-client": "cd nextjs-client && npm run dev",
    "dev": "npm-run-all --print-label --parallel dev:**"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "standard-version": "^9.3.1"
  }
}
